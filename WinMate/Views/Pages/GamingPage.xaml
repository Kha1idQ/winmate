<Page x:Class="WinMate.Views.Pages.GamingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:WinMate.Views.Controls"
      Background="{DynamicResource AppBackgroundBrush}"
      Title="Gaming">
    <Grid Margin="48,32,48,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,12,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="{DynamicResource Gaming_Title}" FontSize="36" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="{DynamicResource Gaming_Subtitle}" FontSize="15"
                           Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0" />
            </StackPanel>
            <ui:Button x:Name="EnableAllButton"
                       Grid.Column="1"
                       Height="48"
                       Padding="20,0,20,0"
                       FontSize="15"
                       VerticalAlignment="Center"
                       BorderThickness="0"
                       Background="{DynamicResource PrimaryButtonBrush}"
                       Foreground="{DynamicResource OnPrimaryBrush}"
                       Content="{DynamicResource Gaming_EnableAll}"
                       Click="EnableAllButton_Click" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <controls:TweakListControl x:Name="TweakList" Margin="0,0,12,0" />
        </ScrollViewer>
    </Grid>
</Page>
