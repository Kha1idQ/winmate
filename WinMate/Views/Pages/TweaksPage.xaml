<Page x:Class="WinMate.Views.Pages.TweaksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:WinMate.Views.Controls"
      Background="{DynamicResource AppBackgroundBrush}"
      Title="Tweaks">
    <Grid Margin="48,32,48,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="{DynamicResource Tweaks_Title}" FontSize="36" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
            <TextBlock Text="{DynamicResource Tweaks_Subtitle}" FontSize="15"
                       Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,8,0,0" />
        </StackPanel>

        <ui:TextBox x:Name="SearchBox"
                    Grid.Row="1"
                    Height="48"
                    Margin="0,20,12,10"
                    FontSize="14"
                    PlaceholderText="{DynamicResource Search_Tweaks}"
                    TextChanged="SearchBox_TextChanged" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <controls:TweakListControl x:Name="TweakList" Margin="0,0,12,0" />
        </ScrollViewer>
    </Grid>
</Page>
