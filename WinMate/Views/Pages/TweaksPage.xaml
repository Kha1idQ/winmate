<Page x:Class="WinMate.Views.Pages.TweaksPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:WinMate.Views.Controls"
      Title="Tweaks">
    <Grid Margin="32,24,32,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="{DynamicResource Tweaks_Title}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource GoldBrush}" />
            <TextBlock Text="{DynamicResource Tweaks_Subtitle}" FontSize="14" Foreground="{DynamicResource GoldDimBrush}" Margin="0,6,0,0" />
        </StackPanel>

        <ui:TextBox x:Name="SearchBox"
                    Grid.Row="1"
                    Margin="0,0,12,12"
                    Icon="{ui:SymbolIcon Search24}"
                    PlaceholderText="{DynamicResource Search_Placeholder}"
                    TextChanged="SearchBox_TextChanged" />

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <controls:TweakListControl x:Name="TweakList" Margin="0,0,12,0" />
        </ScrollViewer>
    </Grid>
</Page>
